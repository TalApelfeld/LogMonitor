// const express = require("express");
// const cors = require("cors");
// const jwt = require("jsonwebtoken");
// const bcrypt = require("bcryptjs");
// const { v4: uuidv4 } = require("uuid");
// require("dotenv").config();

//// In-memory storage (replace with Elasticsearch in production)

//// Generate sample logs

//// Middleware to verify JWT token

//// Role-based authorization middleware

//// Auth Routes
// app.post("/api/auth/register");

// app.post("/api/auth/login");

//// Logs Routes
// app.get("/api/logs", authenticateToken);

// app.get("/api/logs/search", authenticateToken);

// app.post("/api/logs", authenticateToken, authorizeRoles(["admin", "user"]));

// Analytics Routes
// app.get("/api/analytics", authenticateToken, authorizeRoles(["admin", "user"]));

// Users Routes (Admin only)
// app.get("/api/users", authenticateToken, authorizeRoles(["admin"]));

// Health check
// app.get("/api/health");
